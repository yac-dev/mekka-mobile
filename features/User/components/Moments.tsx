import React from 'react';
import { View, Text } from 'react-native';

export const Moments = ({ userId, position, syncOffset, secondRef, onMomentumScrollBegin }) => {
  return (
    <View>
      <Text>Moments</Text>
    </View>
  );
};
