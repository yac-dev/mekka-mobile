import React, { useState, useEffect } from 'react';
import { View, Text } from 'react-native';

const AppInit = () => {
  const [i, l] = useState();
};

export default AppInit;
// ここでuseLoadMe的なことをして帰ってきているdataをここのstateで持つ感じか・・・
